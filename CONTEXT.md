# 项目上下文

## 技术沉淀约定
- 每次功能更新必须补充本文件，沉淀长期有效的术语、架构决策、接口约定、兼容策略、踩坑经验或后续维护注意事项。
- 技术沉淀只记录会影响后续实现和维护的内容，不记录临时进度、命令流水账或一次性调试过程。

## Git 忽略规则约定
- 根级 `.gitignore` 中用于本地资料或发布产物的目录规则必须按意图锚定：仅忽略根目录时使用 `/references/`、`/release/`，避免误伤 `.codex/skills/*/references/`、`.agents/skills/*/references/` 等需要版本管理的技能参考文档。
- Python 字节码、工具缓存、Android `build/` 与 `release/` 打包输出、Gradle/IDE 本地目录和系统 `.DS_Store` 都属于本地生成物，不应提交；若已进入索引，应使用 `git rm --cached` 从版本库移除，保留本地文件即可。

## 管理端手动刮削术语
- `手动刮削`：管理员对已有视频主动发起候选预览、人工选择候选并确认覆盖视频 metadata 的操作，不等同于上传、改类型或后台任务触发的自动刮削。
- 电影手动刮削复用通用刮削页 `/scrape` 和现有 `POST /admin/scrape/preview`、`PUT /admin/scrape/confirm` 接口；详情页入口只通过 query 预填 `video_id`、`type=movie`、`title`，可解析到发行年份时追加 `year`，进入页面后必须由管理员手动点击“查询预览”。
- `电影横向背景`：电影来自 TMDB 的 16:9 背景图，优先对应 `backdrop_path` 或 `backdrop_url`，不同于用于卡片和小海报的竖版 `poster_path`。
- 电影横向背景应本地化保存，TV 端使用本地可访问路径，不直接依赖 TMDB 远程图片作为稳定展示来源。
- 自动电影刮削和管理端手动电影刮削都应产出电影横向背景，避免不同入口得到不一致的 TV 展示素材。
- 电影确认刮削语义是覆盖刮削来源 metadata；在没有 artwork 锁定机制前，重新确认刮削可以覆盖旧电影横向背景。
- `电影重新刮削`：管理员对已有电影再次执行手动刮削预览和确认；预览候选必须来自实时 TMDB 查询，不复用已有视频 metadata 或短期预览缓存。
- `bypass_cache`：管理端手动刮削预览请求中的显式重抓标记；电影手动查询默认使用该标记绕过缓存。

## 演员刮削术语
- `影视演员信息刮削`：电影和电视剧刮削过程中，基于 TMDB credits 得到的演员进一步补齐演员资料并绑定到视频；本语义不覆盖 AV 演员刮削。
- `影视演员资料`：来自 TMDB person 的演员姓名、别名、性别、国家/地区、生日、简介、TMDB person id 与本地头像；不包含角色名。
- `影视演员合并`：影视演员资料优先按 TMDB person id 匹配，其次按姓名匹配；已有头像、手工备注或 AV 来源资料不得被 TMDB 自动刮削覆盖。
- `影视演员落库时机`：影视演员资料只在电影/电视剧确认刮削或自动刮削落库时补齐；候选预览阶段不下载演员头像。
- `影视演员入库范围`：电影/电视剧演员入库按 TMDB credits 返回的演员集合处理，不做人为数量上限；展示层仍可自行限制展示数量。

## 图片上传术语
- `图片上传处理图`：图片上传后用于展示和变体生成的主文件。正常情况下优先转为 WebP；当当前运行环境同时缺少 ffmpeg WebP 编码器和 `cwebp` 时，必须保留原始 JPEG/PNG 等图片作为处理图，上传不能因为 WebP 编码能力缺失而失败。
- `图片变体格式`：图片查看接口按处理图格式生成缩放变体；处理图是 WebP 时生成 WebP，处理图因编码能力缺失保留为 JPEG/PNG 时，变体也沿用 JPEG/PNG，避免访问阶段再次触发不可用的 WebP 编码链路。

## TV 首页术语
- `18+`：面向 TV 端显示的成人内容入口。代码、接口参数和存储内部仍使用 `av`，TV 界面文案不得显示为 `AV`。
- `最近更新`：当前类型下最新可播放内容；电视剧按最近剧集更新，电影和 `18+` 按最新可播放视频。
- `最近播放`：当前类型下用户已有播放进度的内容；电视剧保留季/集信息，电影和 `18+` 指向长视频本体。
- `服务器自动嗅探`：TV 端连接服务器页对局域网服务的扫描过程；扫描状态属于表单内的辅助反馈，应使用小型行内 loading，不使用占据视觉中心的大型加载态。

## TV 首页结构
- 一级首页使用左侧窄菜单和右侧内容区。
- 一级菜单固定为：`电视剧`、`电影`、`18+`、`IPTV`、`搜索`、`设置`，默认选中 `电视剧`。
- `电视剧`、`电影`、`18+` 共享类型化首页结构：巨幅推荐、最近播放、最近更新、全部入口。
- `TV 遥控器体验与视觉一致性`：TV 端第一阶段整体优化优先统一遥控器焦点反馈、可点击元素形态、加载/空态、页面密度和安全留白；不在同一阶段重排首页信息架构、改播放内核或新增内容类型。
- `TV 焦点视觉语言`：TV 端焦点态不使用硬描边或粉红描边。海报/图片卡片聚焦时只放大并保留轻阴影；按钮、菜单、筛选项和频道行使用柔和蓝青色光感或背景提亮；播放器浮层延续夜台玻璃面板的蓝青色焦点光感。
- `TV 10-foot 对比度收口`：TV 端任何文本前景与暗色 surface 之间的对比度必须 ≥ 7.0:1（WCAG AAA），覆盖 `AppChrome.TextPrimary` / `TextSecondary` / `TextMuted` 三档前景，与 `AppChrome.Canvas` / `CanvasRaised` / `Surface` / `SurfaceElevated` / `SurfaceMuted` / `SurfaceStrong` 任意组合都必须达标。`TextMuted = Color(0xFFB0BAC8)`（R176/G186/B194）由 B1 从旧值 `#96A0B2`（旧 ≈ 6.6:1 on `SurfaceElevated`）上抬，覆盖海报卡 `updateText`、详情元信息、`已观看 N%`、`共 N 项内容` 等 34 处 helper 文本场景；不修改 `TextPrimary` / `TextSecondary` / `TextSubtle` 与其它 surface / accent token，避免色域漂移。新增前景色或新增 surface 必须经 `TvColorContrastTest` 的纯函数 `wcagRelativeLuminance(Color)` / `wcagContrastRatio(Color, Color)` 校验通过——公式严格按 WCAG 2.x：sRGB 通道 ≤ 0.03928 时除以 12.92，否则按 `((c + 0.055)/1.055)^2.4` 线性化，亮度 `Y = 0.2126R + 0.7152G + 0.0722B`，contrast = `(L_lighter + 0.05) / (L_darker + 0.05)`，函数对前后景对称取大值/小值。
- `TV 焦点动效物理`：TV 焦点缩放与光晕反馈一律使用 `androidx.compose.animation.core.spring`，禁止再使用 `tween(durationMillis = 140)` 一类的线性近线性曲线作为焦点反馈。spring 参数集中在 `core/ui/TvFocus.kt` 的 `object TvFocusMotionTokens` 暴露：`ScaleDampingRatio = 0.8f`（轻微回弹，保留分量感不过冲）、`ScaleStiffness = 380f`（中速、避免迟滞）适用于焦点放大；`SurfaceDampingRatio = 1f`、`SurfaceStiffness = 620f` 适用于光晕/背景提亮的淡入淡出（critically-damped、比缩放更快，让背景反馈追上 scale 起步）。`PressedScale = 0.97f`（DPad center / Enter / NumPadEnter 按下时下沉目标 scale，0.94–0.99 区间）、`PressDampingRatio = 0.7f`（按下时阻尼略低于焦点放大，按下/回弹更紧凑）、`PressStiffness = 720f`（高于 `ScaleStiffness`，按下与回弹明显比悬停焦点反馈快）适用于按下反馈。`tvFocusableGlow` / `tvFocusableScaleOnly` 内 scale 的 `targetValue` 必须通过纯函数 `resolveTvFocusableScaleTarget(focused, pressed, enabled, focusedScale)` 派生：`!enabled → 1f`，`pressed → PressedScale`，`focused → focusedScale`，否则 `1f`——不允许在 modifier 调用点内联三元表达式。spring 选择必须走 `tvFocusableScaleSpring(pressed)`，`pressed=true` 时使用 `Press*` 组、`pressed=false` 时使用 `Scale*` 组，让按下和悬停切换出现明显刚度差异。`Modifier.onPreviewKeyEvent` 拦截 `Key.DirectionCenter` / `Key.Enter` / `Key.NumPadEnter` 三个 TV 按下键（集中在 `isTvPressKey` 与 `TvPressKeys` 集合，新增按下键必须通过该集合统一注册），`KeyEventType.KeyDown` 翻转 `isPressed = true`、`KeyEventType.KeyUp` 翻转回 `false` 并通过 `performTvPressHapticFeedback(view)` 触发触觉；`onFocusChanged` 失焦时必须主动把 `isPressed` 复位，避免 keyUp 还没到 modifier 就丢失焦点时按下态卡住。触觉常量按 `Build.VERSION.SDK_INT >= Build.VERSION_CODES.R` 守门：API 30+ 用 `HapticFeedbackConstants.CONFIRM`，更低 API 回退 `HapticFeedbackConstants.VIRTUAL_KEY`，避免 CONFIRM 在旧设备上静默失败。`onPreviewKeyEvent` 内统一返回 `false` 表示不吞按键事件，让下游 `focusable()` / `clickable()` / `onClick` 仍能收到 Enter/Center，按下反馈是装饰层不是行为层。新增 TV 焦点反馈动画必须复用这些 token，不允许在调用点硬编码 spring 参数；动效 token 不影响 `focusedScale` / `focusedHaloPaddingDp` 等几何参数。
- `TV 动效时长 token`：TV 端时长驱动的 `tween` 动画（fade、stagger、列表入场、浮层 show/hide 等）必须把 duration 与 easing 收口到 `core/ui/TvMotion.kt` 的 `object TvMotionTokens`，统一落在 200–260ms 区间（TV 端超过 300ms 即感觉迟滞，低于 200ms 即丢失动画感）。当前暴露三档 duration：`DurationFastMs = 200`（小型瞬时反馈，如临时浮层 alpha）、`DurationStandardMs = 240`（默认 TV 过渡时长，如 `LongFormVideoPlayer` 控制条 / seek preview / center feedback 的 `AnimatedVisibility` fade）、`DurationEmphasizedMs = 260`（入场强调，如列表 stagger 单 item 淡入），三档严格升序、Fast < Standard ≤ Emphasized 不允许颠倒。easing 暴露一个 `EasingStandard: Easing = CubicBezierEasing(0.2f, 0f, 0f, 1f)`（Material 标准缓动曲线），不再在调用点写裸 `CubicBezierEasing(...)` 字面量、不允许使用 `LinearEasing` 或纯 `FastOutSlowInEasing`。具体收口位点：`TvListMotionTokens.StaggerEntryDurationMs` / `StaggerEntryEasing` 必须分别引用 `TvMotionTokens.DurationEmphasizedMs` / `TvMotionTokens.EasingStandard`；`LongFormVideoPlayer` 的所有 `fadeIn(...)` / `fadeOut(...)` 必须显式传入 `tween(TvMotionTokens.DurationStandardMs, easing = TvMotionTokens.EasingStandard)`，禁止裸 `fadeIn()` / `fadeOut()` 默认 400ms（超 A5 上限）。spring 系动效（焦点放大 / 按下反馈 / 光晕淡入）继续由 `TvFocusMotionTokens` 提供物理参数，与 duration token 不混用——`TvMotionTokens` 只负责 tween/easing，不负责 spring。新增 TV 时长动画必须从这三档 + 一个 easing 中挑选；如出现确需新 token 的场景（如更紧凑的 micro-interaction），应扩 `TvMotionTokens` 而不是在调用点硬编码数字。
- `TV 10-foot 排版 token`：TV 端 200+ 处 `MaterialTheme.typography.<role>` 调用点全部继承 `core/ui/TvTypography.kt` 暴露的覆写实例 `val TvTypography: Typography`，注入点唯一——`TvShellApp` 顶层 `MaterialTheme(colorScheme = AppDarkColors, typography = TvTypography) { ... }`，不允许在调用点硬编码 `fontSize = X.sp` 字面量。10-foot 视距阈值由 `object TvTypographyTokens` 暴露三档常量：`MainTitleSp = 34`（主标题地板，hero 标题）、`SubtitleFloorSp = 22`（副标题地板，海报卡 `titleSmall` / section 标题）、`HelperFloorSp = 18`（助记/标签/body 地板）、`TightHelperSp = 18`（`labelSmall` 兜底，保持 ≥18sp）。13 个 Material3 type role 的实际数值：`headlineLarge = 34/42`、`headlineMedium = 30/38`、`headlineSmall = 28/36`、`titleLarge = 26/32`、`titleMedium = 24/30 letterSpacing=0.15`、`titleSmall = 22/28 letterSpacing=0.1`、`bodyLarge = 20/28 letterSpacing=0.5`、`bodyMedium = 18/24 letterSpacing=0.25`、`bodySmall = 18/22 letterSpacing=0.4`、`labelLarge = 18/22 letterSpacing=0.1`、`labelMedium = 18/22 letterSpacing=0.5`、`labelSmall = 18/22 letterSpacing=0.5`；fontSize/lineHeight/letterSpacing 必须保持 lineHeight ≥ fontSize 且 heading→title→body 各档单调不增。`display*` 三个 role 在 TV 工程零调用点，保留 Material3 默认值不显式覆写，避免引入未使用常量。新增 role 或新增 sp 档必须扩 `TvTypographyTokens` 并同步落入 `TvTypography` 实例，不允许在 `Text(style = ..., fontSize = X.sp)` 处直接写字面量绕过 token。回归测试 `TvTypographySpecTest` 通过反射 `TvTypography.<role>.fontSize.value` 校验地板、单调性、line-height 安全余量；并通过源代码字符串断言锁定 `TvShellApp.kt` 必须包含 `typography = TvTypography` 与 import。
- `TV 焦点双层 glow`：TV 焦点态视觉由两层 tinted glow 共同表达，二者参数集中在 `core/ui/TvFocus.kt` 的 `object TvFocusMotionTokens` 暴露：`InnerGlowAlphaTarget: Float = 0.6f`（内层背景提亮的目标 alpha，0.5–0.75 区间）控制 `tvFocusableGlow` 的 `.background(TvFocusGlowColor.copy(alpha = InnerGlowAlphaTarget * surfaceAlpha))`——`surfaceAlpha` 是聚焦淡入因子，`InnerGlowAlphaTarget` 是稳态目标，禁止再像 0.1.48 之前那样用 `Color(0x2639D7E8)` 这类把 alpha 写死在色值里的字面量；`OuterHaloElevationDp: Dp = 12.dp`（外层 tinted halo 的 elevation，8–16dp 区间，必须与 `TvFocusSafeSpec.posterFocusSafeSpaceDp = 8f` 配合保证不漏到邻卡）控制 `tvFocusableGlow` 与 `tvFocusableScaleOnly` 末端的 `Modifier.shadow(elevation = haloElevation, shape, clip = false, ambientColor = TvFocusGlowColor, spotColor = TvFocusGlowColor)`——`Modifier.shadow` 的 `ambientColor` / `spotColor` 在 API 28+ 平台 path 被 honor 拿到 cyan tint，API 26-27 自动回退到默认黑灰 graceful degradation（不崩溃、不报错，视觉略弱）。`haloElevation` 必须走 `animateDpAsState(... spring(SurfaceDampingRatio, SurfaceStiffness))` 与内层 `surfaceAlpha` 同节奏淡入淡出，禁止 jump cut。modifier 链顺序强约束：`graphicsLayer { scale }` → `Modifier.shadow(...)` → `.background(...)`（仅 `tvFocusableGlow`） → `.focusable()`——scale 必须在 shadow 之前才能让 halo 跟着卡片视觉中心放大；shadow 在 background 之前才能让外层光晕作用于内层提亮；`clip = false` 必须保持，halo 才能溢出 bounding box 形成扩散。`tvFocusableScaleOnly` 只接外层 halo + scale、**不接**内层 `.background(...)`——它用于海报卡，内层提亮会遮挡海报图。已禁止的回归模式：调用点裸 `graphicsLayer { shadowElevation = 32f/28f }`（黑灰 shadow，无 tint）、裸 `Color(0x2639D7E8)`（alpha 写死）、不走 token 的 hard-coded `0.6f` / `12.dp` 字面量。回归测试 `TvFocusSpecTest` 通过源文字符串断言锁定 `Modifier.shadow(` 出现 ≥2 次、`ambientColor / spotColor = TvFocusGlowColor`、`animateDpAsState(`、token 引用、`clip = false`、且不再出现旧 `shadowElevation = 32f/28f` 与 `Color(0x2639D7E8)`。
- `TV 焦点 ISE 三层防线`：TV 端 `FocusRequester.requestFocus()` 因目标节点未挂载抛出 `IllegalStateException: FocusRequester is not initialized` 是 Compose 1.7 异步协程恢复路径的固有失败模式（栈穿过 `BaseContinuationImpl.resumeWith` → `DispatchedTask.run` → `AndroidUiDispatcher` 异步透到主 Looper），不能只靠单一调用点 try-catch 覆盖。整体防线由三层叠加构成、层层独立、缺一不可，禁止移除任何一层：第一层「同步」`core/ui/TvInitialFocusEffect.kt:24-35` 的 `fun FocusRequester.tryRequestFocus(): Boolean` 是业务侧唯一入口——内部同步 try-catch `IllegalStateException`，调用 `isFocusRequesterNotInitialized(err)` 关键字匹配命中即吞掉返回 `false`，未命中重抛；强约束**`LaunchedTvInitialFocus { ... }` 块体内必须用 `.tryRequestFocus()` 而不能裸调 `.requestFocus()`**（一次性事件回调如点击 / 按键 / 动画完成除外，那些路径上 try-catch 帧能正常生效）。第二层「协程」`core/ui/TvInitialFocusEffect.kt:37-53` 的 `@Composable LaunchedTvInitialFocus(vararg keys, block)` 在 `withFrameNanos { }` 等过一帧后跑 `runCatching { block() }.onFailure { ... }`：`CancellationException` 必须重抛以保留协程取消语义，focus-requester ISE 命中即吞掉，其他异常继续抛——这一层吞掉 R8 内联 / 异步恢复路径绕开 try-catch 帧时的兜底 ISE。第三层「主 Looper」`tv/TvMainActivity.kt` 的 `installMainLooperHoverExitGuard()`：`Handler.post { while(true) try Looper.loop() catch (err) { ... } }`，匹配 `shouldSwallowTvComposeHoverExitCrash(err)` **或** `shouldSwallowTvComposeFocusRequesterCrash(err)` 任一即吞，否则原样 `throw err`。focus-requester matcher 判别签名锁定两个充要条件：`err is IllegalStateException` 且 `err.message?.contains("FocusRequester is not initialized") == true`、栈中至少一帧 `frame.className.startsWith("androidx.compose.ui.focus.")`（不绑定具体方法名，因为 `FocusRequester` / `FocusOwnerImpl` / `findFocusTargetNode$ui_release` 等帧在不同 Compose 版本与 D8 lambda 合成路径下保留情况不一致）；非 Compose focus 子系统抛出的同消息异常必须继续重抛，回归测试 `TvMainActivityInputPolicyTest` 的 `does not swallow focus requester message from non compose source` / `does not swallow focus requester message when stack lacks compose focus package` 锁定误伤边界。该三层防线交集为：业务正常路径走第 1 层即返回，R8 内联 / 异步路径走第 2 层兜底，第 3 层永远兜底主 Looper——任何一层失效仍能整体防住 FATAL。详见 [`TV 焦点请求安全调用 tryRequestFocus`] 第 1 层细则、[`TV 初始焦点请求约束`] 第 2 层细则、[`TV 主 Looper FocusRequester 未初始化兜底`] 第 3 层细则。
- `TV 图标操作`：TV 端圆形或小型图标按钮属于按钮焦点语言的一部分，应使用共享 TV 图标操作组件承载点击、尺寸和青蓝焦点反馈；不要把默认 Material `IconButton` 作为主要 TV 焦点控件。用于接收遥控按键的根焦点容器不属于图标操作。
- `TV 电视剧详情页操作`：电视剧详情页的返回、播放、季选择和集选择都属于遥控器可达的详情页操作。返回应复用共享 TV 图标操作；播放、季选择和集选择继续使用共享焦点视觉，不使用默认 Material 按钮作为主要焦点控件。
- `TV 长视频详情页操作`：电影和 `18+` 的沉浸式详情首屏中，返回、播放和收藏都属于遥控器可达的详情页操作。返回应复用共享 TV 图标操作；播放和收藏继续使用共享焦点视觉，不使用默认 Material 按钮作为主要焦点控件。
- `TV 第一阶段视觉覆盖范围`：TV 遥控器体验与视觉一致性覆盖首页左侧菜单、首页内容卡片、搜索、设置、海报墙、电影/`18+` 详情页、电视剧详情页、IPTV 频道列表、播放器音轨/字幕/返回提示等浮层控件；不覆盖播放内核、解码策略、播放历史上报、IPTV 播放引擎选择或首页信息架构重排。
- `TV 工程编译边界`：TV 独立工程的主编译图只应包含 TV 启动、连接/配对、长视频/电视剧、`18+`、IPTV、TV 设置和这些能力直接复用的基础设施；手机端首页、短视频、图片合集、Mine、上传或手机端互动流可以暂留源码目录作迁移参考，但必须排除在 TV 编译图之外。
- `TV 状态反馈语言`：TV 端加载、空态和错误态应使用统一状态组件表达。页面级加载使用居中紧凑状态，列表分页加载使用行内状态，空态说明保持短句，错误态必须提供可聚焦的重试动作；服务器自动嗅探继续使用表单内小型行内 loading，不升级为页面级加载态。
- `TV 配对与服务器连接体验`：根启动、配对登录和切换服务器属于进入 TV 首页前的同一条连接链路。根启动应使用共享页面级状态反馈；配对页主操作和切换服务器操作应使用 TV 共享焦点视觉且每个操作只有一个焦点目标；服务器自动嗅探仍属于表单内辅助反馈，保持小型行内 loading。
- `TV 服务器连接页`：连接服务器页属于 TV 进入首页前的配置界面，自动嗅探、手动填写和历史地址应使用 TV 深色面板风格；重新扫描、测试并保存、使用、连接、删除等操作应使用共享 TV 焦点视觉，不使用默认 Material 按钮焦点作为主要反馈。
- `TV 滚动内容底部安全留白`：TV 端可滚动内容页的底部应保留可感知安全空间，避免最后一个焦点项贴近屏幕底边；不适用于播放器视频画面、沉浸式详情首屏、加载态或居中空状态。
- `TV 安全区域顶部留白`：TV App 主 Activity 启用了 edge-to-edge（`enableEdgeToEdge(statusBarStyle = SystemBarStyle.dark(TRANSPARENT), navigationBarStyle = SystemBarStyle.dark(TRANSPARENT))`），所有进入 TV 首页之后的非沉浸式页面级 root（一级首页 `TvCatalogScreen`、海报墙 `TvPosterWallScreen`、电视剧详情 `TvSeriesDetailScreen`、配对页 `TvPairingScreen` 等）都必须在最外层 `Box`/`Column`/`Row` 上叠加 `Modifier.statusBarsPadding()`，否则顶部内容会绘制在状态栏/安全区域下方，呈现为「头部被裁切只能看到一半」；不允许只在子组件局部加 padding 或依赖 `safeContentPadding()` 等差异化封装。沉浸式详情首屏（`TvLongFormDetailScreen` 电影/`18+`）走另一套分层规则：最外层 `Box` 不能叠 `statusBarsPadding()`，否则底层横向背景图（`TvLongFormDetailBackground` 的 `AsyncImage`）会跟着被状态栏高度压下去、顶部出现一条状态栏色块，破坏「画到屏幕顶端」的沉浸感；状态栏 inset 由前景元素自带（如返回 `TvIconActionButton` 直接在 `Modifier.align(Alignment.TopStart)` 之后串 `.statusBarsPadding()`），保证背景延伸到顶端、可聚焦操作仍落在安全区域内。播放器全屏画面、根启动加载、QR/配对二维码等本身就需要顶到屏幕边缘的视图属于例外，由该视图自行声明，不套用本规则。
- `TV 海报墙排序`：TV 海报墙排序必须由服务端分页接口执行，不能只排序当前页；排序维度为添加时间和发售时间，方向为正序和倒序。电影/`18+` 添加时间指视频入库时间，发售时间指 metadata 发售日期；电视剧发售时间指首播日期，添加时间指该剧可播放视频的最新入库时间。缺失排序日期的条目排在最后。`SearchVideosOrdered` 用 EXISTS 子查询匹配 `video_tags`，不再使用 `LEFT JOIN video_tags + SELECT DISTINCT` 的组合；Postgres 在 `SELECT DISTINCT` 下要求 `ORDER BY` 表达式必须出现在 SELECT 列表里（错误码 42P10），而发售时间排序使用的 `NULLIF(v.metadata->>'release_date', '')::date` 不可能也不应该写进 SELECT 列表，因此结构上消除 DISTINCT。
- 左侧菜单只属于 `tv-home` 一级页；海报墙、详情页、播放器页保持现有全屏返回体验。
- `TV 根退出确认`：TV App 仅在 `tv-home` 根页面拦截系统返回键，第一次返回显示“再按一次退出”，确认窗口内第二次返回才退出应用；海报墙、详情页和播放器页继续使用各自既有返回语义。
- TV 首页左侧菜单按钮只能有一个焦点目标；`tvFocusableGlow()` 已经包含 `focusable()`，按钮自身不要再额外叠加 `.focusable()`，否则遥控确认键可能先落到重复焦点层，表现为必须按两次才触发菜单动作。
- `TV 初始焦点请求约束`：TV 首页初始焦点只能请求当前已经组合到界面树里的节点。默认首页内容为空或接口失败导致没有巨幅推荐/最近播放/货架时，应回退到左侧菜单焦点；不要请求仅在“搜索”菜单下才会组合的搜索框焦点，否则 Compose 会因为 `FocusRequester` 未绑定节点在冷启动时崩溃。即使目标节点会出现，`LazyColumn` 等延迟组合容器在 `LaunchedEffect` 首次触发时可能尚未挂载该 item 的 `Modifier.focusRequester` 节点，直接 `requestFocus()` 仍会抛 `IllegalStateException: FocusRequester is not initialized`。所有 TV 初始焦点请求必须通过共享 helper `LaunchedTvInitialFocus(vararg keys, block)` 执行：helper 内部先 `withFrameNanos { }` 等过一帧让 Compose 走完一次组合，再 `runCatching { block() }` 精确过滤 `IllegalStateException` 且 `message` 包含 `FocusRequester is not initialized` 关键字的异常（Compose 1.6 抛出的 message 是带前导换行和缩进的 raw multiline 字符串，必须用 `contains` 不能用 `startsWith`，否则匹配漏失会让真实 ISE 透出导致崩溃），`CancellationException` 必须重抛以保留协程取消语义，其他异常继续抛出。不允许在业务 `LaunchedEffect` 里裸调 `focusRequester.requestFocus()`。
- `TV 焦点请求安全调用 tryRequestFocus`：Compose 1.7（含 BOM 2024.10.01 + UI 1.7.4）将 `LaunchedEffect` 的 suspend 协程通过 `BaseContinuationImpl.resumeWith` → `DispatchedTask.run` → `AndroidUiDispatcher` 异步恢复，恢复时抛出的 `IllegalStateException` 在某些路径下不会回到 `LaunchedTvInitialFocus` 外层 `runCatching` 的 try-catch 帧，而是直接透到主 Looper，被 `TvMainActivity.installMainLooperHoverExitGuard` 之外捕获不到而 FATAL（实测复现路径：海报墙/详情/IPTV/配对页冷启动时第一次 `requestFocus()` 节点未挂载）。`core/ui/TvInitialFocusEffect.kt` 暴露 `fun FocusRequester.tryRequestFocus(): Boolean` 作为唯一安全入口：内部同步 try-catch `IllegalStateException` 并复用 `isFocusRequesterNotInitialized` 关键字匹配；命中即吞掉返回 `false`，未命中原样重抛。约定：`LaunchedTvInitialFocus { ... }` 块体内**只能**用 `.tryRequestFocus()` 而不能裸调 `.requestFocus()`；其它一次性事件回调（点击、按键、动画完成）仍可继续使用 `.requestFocus()`，因为这些路径上 try-catch 帧能正常生效，不需要额外兜底。回归测试通过源代码字符串断言（`TvInitialFocusSafeRequestTest`、`TvCatalogFocusPolicyTest`）锁定 helper 文件与各调用点必须出现 `tryRequestFocus` 字样。
- `TV 电视剧海报 shared-element 过渡`：TV 端电视剧海报墙跳转电视剧详情页使用 Compose 1.7 的 `SharedTransitionLayout` + `sharedElement` 做共享元素过渡，让用户感知到“点的这张海报放大变成详情页里那张海报”。统一入口集中在 `core/ui/TvSharedPoster.kt`：`LocalTvSharedTransitionScope`（值类型 `SharedTransitionScope?`，默认 `null`）由 `TvShellApp` 在 `SharedTransitionLayout` 内通过 `CompositionLocalProvider(... provides this@SharedTransitionLayout)` 提供；`LocalTvAnimatedContentScope`（值类型 `AnimatedContentScope?`，默认 `null`）由 `TvShellApp` 在 `composable(TvCatalogWallRoutePattern, ...) { ... }` 与 `composable(TvSeriesRoutePattern, ...) { ... }` 两个目的页面块体内通过 `CompositionLocalProvider(... provides this@composable)` 提供，其他 destination 不注入，避免无关页面承担实验 API 成本。绑定通过 `fun Modifier.tvSharedSeriesPoster(seriesId: String): Modifier` 完成：内部读两个 `CompositionLocal`，任一为 `null` 时直接 `return this`（包括非 TV 工程引用、单元测试以及未被 SharedTransitionLayout 包裹的环境），命中则用 `with(sharedScope) { sharedElement(state = rememberSharedContentState(key = "tv-series-poster-$seriesId"), animatedVisibilityScope = animatedScope) }`。命名空间前缀 `tv-series-poster-` 强制约定，禁止与其他 shared key 撞键。调用点：海报墙 `TvPosterWallCard` 内 9:16 海报 `Box` 在 `item.type == "tv"` 时叠加该 modifier（电影/`18+` 走的是 LongFormDetail，A2 不开放）；详情页 `TvSeriesDetailScreen` 的 228dp / 2:3 / RoundedCornerShape(26.dp) 海报无论是否有 `posterUrl` 都叠加该 modifier，两端 key 对齐到 `series.id`。共享 `Modifier` 必须放在 `.clip(...)` 之前，否则裁剪会和 shared-element 的 bounds resize 冲突造成裁切跳变。所有 shared-element API 都是实验性的，新增/移动调用点必须保留 `@OptIn(ExperimentalSharedTransitionApi::class)`。回归测试 `TvSharedPosterTransitionSpecTest` 通过源代码字符串断言锁定 helper、TvShellApp 包装、海报墙调用点与详情页调用点的存在与命名一致性。
- `TV 列表入场 stagger`：TV 端 `LazyColumn` / `LazyVerticalGrid` 首屏成 item 入场需要按 index 错开淡入/上移，避免一次性整屏弹现造成的"瞬间满屏 + 焦点视觉重置"。集中在 `core/ui/TvListMotion.kt`：`object TvListMotionTokens` 暴露 `StaggerPerItemMs = 35L`（单项间隔，落在 25–50ms TV 可感知区间）、`StaggerEntryDurationMs = 260`（单项淡入时长，落在 A5 计划要求的 200–280ms 区间内）、`StaggerEntryDistanceDp = 12.dp`（上移距离，控制在 8–20dp 避免视线追踪疲劳）、`StaggerMaxSteps = 12`（阶梯上限，避免深处滚动 item 等待 1s+）、`StaggerEntryEasing = CubicBezierEasing(0.2f, 0f, 0f, 1f)`（强制 cubic，不允许 linear）。`fun tvStaggerEntryDelayMs(index, ...): Long` 为纯函数，对 `index` 做 `coerceIn(0, maxSteps)` 后乘以 `perItemDelayMs`，单测可直接验证；`@Composable fun Modifier.tvStaggerEntry(index): Modifier` 内部 `LaunchedEffect(Unit)` 调用 `delay(tvStaggerEntryDelayMs(index))` 后翻转 `visible` 触发 `animateFloatAsState`，通过 `graphicsLayer { alpha = progress; translationY = (1 - progress) * distancePx }` 渲染，**禁止**改用 `Modifier.alpha` / `Modifier.offset` 之类会触发布局回流的修饰器。调用点：`TvPosterWallScreen` 的 `LazyVerticalGrid` 必须用 `itemsIndexed` 取出 index 后在 `TvPosterWallCard` 外层 `modifier` 链尾叠 `.tvStaggerEntry(index)`，本身的 `focusRequester` 必须放在 stagger **之前**才能保证首项焦点请求时节点已挂载。其他 TV LazyColumn / LazyVerticalGrid（IPTV 频道行、TV 首页 shelves、电视剧详情 episodes 网格）按需复用，但每次接入都必须通过 `tvStaggerEntry` 入口，不允许在调用点硬编码 alpha/translation 动画或拷贝 token 数值。回归测试 `TvListMotionSpecTest` 锁定 token 数值区间、`tvStaggerEntryDelayMs` 的夹紧语义、helper 文件结构以及 `TvPosterWallScreen` 的导入与调用点。
- `TV hover 输入兼容兜底`：部分 Android TV 设备会向 Compose 根视图发送 hover/generic motion 事件，并触发 Compose 平台层 `ACTION_HOVER_EXIT` 未清理异常；该异常不代表业务状态损坏，可在 Activity 输入边界按精确异常消息和 Compose 根视图堆栈做窄范围吞掉，其他输入异常必须继续暴露。Compose 平台层会通过 `Handler.post` 把 hover-exit 校验 lambda 调度到主 Looper 异步执行，调用栈不会再经过 `dispatchGenericMotionEvent`，因此兜底需要同时覆盖 Activity 输入边界和主 Looper：在主线程 `Handler.post` 一个外层 `Looper.loop()` try/catch 循环，仅吞掉同样匹配 `AndroidComposeView.sendHoverExitEvent` / `dispatchHoverEvent` 及其 `$lambda$` 合成方法名的特定异常，其他异常继续抛出。匹配方法名需要把合成 lambda 名（如 `sendHoverExitEvent$lambda$5`）一并纳入，否则 D8 生成的同步 lambda 帧会绕过过滤。
- `TV 主 Looper FocusRequester 未初始化兜底`：Compose 1.7 在 `LaunchedTvInitialFocus` 协程恢复路径上调用 `requestFocus()` 抛出 `FocusRequester is not initialized` 时，异常可能绕过 `tryRequestFocus()` 同步 try-catch（实测路径：`FocusRequester.focus$ui_release` → `BaseContinuationImpl.resumeWith` → `DispatchedTask.run` → `AndroidUiDispatcher` 异步透到主 Looper）。同样地，遥控器 DPad 触发 Compose 内部 `FocusOwnerImpl.focusSearch-ULY8qGw` → `FocusRequester.findFocusTargetNode$ui_release` 时若目标节点未挂载也会抛同名 ISE，并通过 `AndroidComposeView$keyInputModifier$1.invoke-ZmokQxo` → `Activity.dispatchKeyEvent` → IMM/ViewRootImpl input pipeline 同步透回主 Looper。`TvMainActivity.installMainLooperHoverExitGuard` 的 `Looper.loop()` try/catch 循环必须并列匹配 `shouldSwallowTvComposeFocusRequesterCrash`：判定条件分两层——主安全网是 `IllegalStateException` 且消息 `contains("FocusRequester is not initialized")`；栈帧条件只要求至少一帧 `className.startsWith("androidx.compose.ui.focus.")`，**不再绑定到具体 FocusRequester 方法名**（`requestFocus` / `focus$ui_release` / `findFocusTargetNode$ui_release`），因为 Compose 在不同异步/同步路径下保留的栈帧组合不一致（`FocusOwnerImpl` 帧可能单独出现而 `FocusRequester` 帧被剥离）；放宽到 focus 包前缀让兜底跨 Compose 版本与 D8 lambda 合成都鲁棒。该兜底是 hover-exit 兜底的对偶项，作用域同样限定在主 Looper，业务侧 `tryRequestFocus` 仍是首要防线、不替换；非 Compose focus 子系统抛出的同消息异常（业务源帧、`AndroidComposeView.dispatchKeyEvent` 单独栈等）必须继续抛出，回归测试 `TvMainActivityInputPolicyTest.does not swallow focus requester message from non compose source` / `does not swallow focus requester message when stack lacks compose focus package` 锁定误伤边界，正向用例见同测试 `swallows compose focus requester not initialized from request focus path` / `swallows compose focus requester crash from focus search dpad path` / `swallows focus requester crash with only focus owner impl frame`。
- `TV BringIntoView 最小滚动策略`：Compose Foundation 1.7 `androidx.compose.foundation.gestures.LocalBringIntoViewSpec` 在 leanback 设备（`PackageManager.FEATURE_LEANBACK`）上默认计算为 `PivotBringIntoViewSpec`，对任何焦点目标都会返回 `leadingEdgeOfItemRequestingFocus - 0.3 * containerSize` 的非零位移——**即使该目标已经完整可见**，仍会触发 `tween(125ms, CubicBezierEasing(0.25f,0.1f,0.25f,1f))` 的滚动动画把目标拉到容器 30% pivot 位置。TV 首页初始焦点落在 `TvFeaturedHero`（324dp 横幅）底部播放按钮 `TvHeroActionButton` 时，pivot 行为把整个 `LazyColumn` 上滑约 110dp，外观为「大海报头部被裁切」；详情页/海报墙等含子滚动（LazyRow 演员行、LazyVerticalGrid 海报网格、IPTV 频道行）的页面也会被同一 spec 影响。修复方式：`core/ui/TvBringIntoView.kt` 暴露 `val TvMinimalBringIntoViewSpec: BringIntoViewSpec`，其 `calculateScrollDistance(offset, size, containerSize): Float` 复刻 `BringIntoViewSpec.Companion.defaultCalculateScrollDistance` 的「最少滚动」语义——目标完整可见（`leadingEdge >= 0 && trailingEdge <= containerSize`）或目标已横跨容器（`leadingEdge < 0 && trailingEdge > containerSize`）返回 0；否则选 leadingEdge / trailingEdge - containerSize 中绝对值更小的一边作为滚动位移。注入点：`TvShellApp.TvAuthenticatedNav` 在 `SharedTransitionLayout` 外层包一层 `CompositionLocalProvider(LocalBringIntoViewSpec provides TvMinimalBringIntoViewSpec)`，作用域覆盖所有 TV 屏幕；调用方需在 `@OptIn(ExperimentalFoundationApi::class)`，因 `LocalBringIntoViewSpec` / `BringIntoViewSpec` 仍属实验 API。该策略替代旧的 `TvCatalogScreen.contentLazyListState + scrollToItem(0,0) + hasPinnedInitialScroll` 钉位兜底（钉位标志在数据加载阶段 `initialFocusTarget=MENU` 第一次 effect 触发时会提前翻 true，导致 featured 数据到位后切到 FEATURED 焦点时钉位被跳过，pivot 仍能上滑——根本修法是消除 pivot 而非追着回滚）。回归测试 `TvBringIntoViewSpecTest` 覆盖完全可见返回 0、跨容器返回 0、超出顶部/底部的最小滚动、双侧超出取最小、`TvMinimalBringIntoViewSpec.calculateScrollDistance` 与纯函数一致；任何 TV 屏幕需要恢复 pivot 行为只能在该子树内重新 provide spec，不允许在 `TvShellApp` 入口移除该 spec 注入。

## TV 端 AV 海报约定
- TV 端 `18+` 的大背景指首页/分类页巨幅推荐背景和详情页顶部背景，应优先使用 AV 元数据中的原始横幅海报 `poster_original_path`。
- TV API 给首页和分类巨幅推荐使用的 AV `backdrop_url` 由后端统一解析；fallback 顺序为 `poster_original_path`、`poster_url`/`poster_path`、抓取来源块内的 `poster_url`/`poster_path`、`poster_cropped_path`、`thumbnail_path`。
- `TV 海报墙海报卡`：TV 海报墙使用 9:16 竖向海报卡，图片区域不做内边距，标题紧贴图片下方并使用深色底；AV 类型标题使用后端返回的番号式 `title` 展示。聚焦态只允许放大，不使用描边式焦点边框。
- AV 详情页左侧小海报继续使用缩略图或裁剪图，不改成横幅图，避免破坏竖向卡片比例。

## TV 沉浸式详情首屏
- `沉浸式详情首屏`：TV 端电影和 `18+` 共用的长视频详情页首屏样式，使用全屏横向背景和底部半透明信息面板承载标题、年份/时长/标签、简介、演员头像与播放/收藏操作；电视剧详情页不套用此约定。
- 长视频详情背景优先使用可识别的横向背景字段：电影使用 `backdrop_url`、`backdrop_path`、`fanart_url`、`fanart_path`；`18+` 保持 AV 原始横幅海报优先规则。缺少横向背景时才允许使用竖向海报作为模糊暗化兜底，并通过展示模型显式标记为兜底，避免把竖海报当作正常横幅裁切。
- TV 电影详情页必须使用已下载到本地的电影横向背景；TMDB 原始相对图路径不能直接作为 TV 背景候选。后端应通过视频图片本地访问路由暴露电影横向背景，TV 端不依赖服务端文件系统路径。
- TV 电影/`18+` 详情页操作只保留“播放”和“收藏/取消收藏”；收藏状态复用详情 DTO 的 `user_state.is_favorited` 与 `DetailViewModel.toggleFavorite()`，不新增分享入口或更多信息滚动入口。
- 详情页演员区最多展示 5 个演员，优先使用接口 `actors.avatar_url`，无头像时显示圆形文字占位；无演员数据时隐藏演员区，不发起额外抓取。

## TV 播放术语
- `运行时切轨`：TV 长视频播放过程中选择音轨后，当前播放音频应立即切换到目标音轨，并在音轨列表中反映当前播放音轨。
- `夜台玻璃面板`：TV 播放器中的居中深色半透明玻璃浮层，背景保持可感知但压暗，边缘带细高光，焦点态使用柔和蓝青色光感；字幕和音轨选择共用这一视觉语言。
- `TV 播放器退出确认`：TV 长视频和电视剧播放器内的系统返回键、控制条返回详情和退出播放操作都属于退出当前播放的用户意图，应进入同一套“第一次提示、第二次确认退出”流程；不允许控制条按钮绕过确认直接离开播放。
- `自动选择`：TV 音轨选择中的默认项，表示跟随视频默认音轨，不等同于某一条固定音轨。
- `快进/快退步长`：TV 长视频播放器左右键单次跳转的秒数，属于全局播放设置；可选值为 5、10、15、20、30 秒，默认 10 秒，同时作用于右键快进和左键快退，长按/重复按保持按步长倍数加速。
- `连按合并跳转`：TV 长视频播放器在用户连续触发快进/快退时，界面应即时显示累计目标位置，但实际播放跳转应在停止按键约 300ms 后只执行一次；方向切换时以当前累计目标继续计算，避免连续 `seekTo` 造成播放卡顿。

## IPTV 术语
- `IPTV 播放列表`：后台维护的一份全局 M3U/M3U8 频道源；本期只允许一个播放列表生效，上传文件或远程刷新都会替换当前频道清单。
- `频道/电视台`：M3U 中一条可播放的直播流，必须包含频道名和 `http/https` 播放 URL；`group-title`、`tvg-logo`、`tvg-id` 是可选信息。
- `频道分组`：M3U 的 `group-title`；TV 端频道列表按分组展示，缺失分组时显示为 `未分组`。
- `当前播放频道`：TV 端 IPTV 播放页正在直连播放的频道；默认使用频道清单第一项，遥控器上下键按原始顺序首尾循环切换。

## IPTV 接口约定
- 后端负责保存和解析 M3U，Admin Web 负责上传文件、保存远程 URL、手动刷新和预览解析结果；TV App 只获取解析后的频道清单。
- TV App 播放 IPTV 时直接连接 M3U 中的原始频道 URL；后端不代理、不转码、不隐藏源地址。
- 远程 M3U URL 只在后台手动刷新时拉取，本期不做定时刷新和 EPG 节目单。
- TV App 播放 M3U8/HLS 频道必须打包 Media3 HLS 扩展模块；仅引入 `media3-exoplayer` 时，`DefaultMediaSourceFactory` 会在运行时找不到 `HlsMediaSource.Factory` 并导致 IPTV 播放页崩溃。
- Media3 `PlayerView` 的 `texture_view` 只能排除部分 Compose/Surface 渲染黑屏；如果 IPTV 日志中音频解码器已初始化但没有视频解码器初始化，且出现多个 `VideoCapabilities` 不支持提示，应按直播源视频编码兼容性处理。
- TV App 的 IPTV 播放页使用 LibVLC `VLCVideoLayout` 独立播放，在 Compose `AndroidView` 中必须让 LibVLC 使用 TextureView 输出，并关闭硬解优先走软解；其他长视频播放器继续使用 Media3。IPTV 源常见 MPEG2、特殊 H.264 profile、Dolby Vision 或设备厂商解码缺口，不能只依赖 Android TV 设备硬解能力。
- IPTV 播放无画面排查必须保留播放事件诊断：至少记录 LibVLC event、vout 数、视频轨/音频轨数量和当前视频轨 codec/分辨率。若 `videoTracks=0` 或无 `Vout`，优先检查 M3U 频道 URL、源站多码率/分片返回和是否需要后端转码；若有视频轨和 `Vout` 但仍黑屏，优先检查输出视图/系统合成层。
- IPTV 频道清单不应包含音频专用源。明显音频源包括 `group-title=Audio/音频`、频道名包含 `音频` 或 `audio only`、URL 路径包含 `/audio/`、`_audio/`，以及 `.mp3/.aac/.m4a/.flac/.wav/.ogg/.opus` 等音频文件。后端解析时应跳过，TV 端也要过滤旧数据，避免默认播放只有声音没有画面的条目。
- TV App IPTV 顶部频道信息不是常驻条：进入播放页和切换频道后仅显示 3 秒临时提示，频道列表打开、加载中、无可播放频道、状态错误或播放错误时必须隐藏。临时提示仍使用后端返回的 `logo_url`（M3U `tvg-logo`）展示台标，`logo_url` 为空或图片加载失败时使用 TV 图标回退，不新增占位资源。
- IPTV 频道列表的 `LazyColumn` item 顺序固定为：标题、分组标题、频道行。遥控器打开列表时应直接初始化到当前播放频道附近，不应先从顶部出现再动画滚动；焦点上下移动时必须按频道 id 映射到实际 item index 并保留短动画滚动到可见区域，避免分组标题导致索引偏移。
- TV App 分发仍使用 APK，不切换 AAB；由于 IPTV 播放兼容性优先保留 `libvlc-all`，APK 必须按 ARM ABI 拆分输出 `armeabi-v7a` 和 `arm64-v8a` 两个安装包，不生成 x86/x86_64 或 universal APK。手动侧载时按设备 ABI 选择对应 APK；仓库不保存 keystore，Release 签名继续由 Android Studio 本机签名配置完成。
